# UI/UX 指南

- 可点元素 ≥ 44px（移动主按钮 ≥ 48px）
- 全站禁止 emoji；图标见 `Cosmos/icons.js`
- 知识卡渐进披露；导览同时仅 1 张卡
- 对比度 ≥ 4.5:1；图标按钮需 `aria-label`
- `prefers-reduced-motion`：跳过 preloader 闪烁、导览相机 instant cut

配色 token 见 `src/assets/design-tokens.css`。
